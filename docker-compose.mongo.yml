services:
  mongodb:
    image: mongo:8.0
    container_name: aviasales-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: aviasales
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
